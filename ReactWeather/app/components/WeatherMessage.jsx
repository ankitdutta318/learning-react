const React = require('react');

const WeatherMessage = React.createClass({
    render: function() {
        return (
            <h4>It's 36C in Jamshedpur</h4>
        );
    }
});

module.exports = WeatherMessage;